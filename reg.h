#include <iostream>
#include <stdio.h>
#include <string.h>
#include <math.h>
using namespace std;
void halo (void);
int convert_to_decimalu (string s);
//bool checklabel (vector<string> );
bool check_label (string s);
int convert_to_decimal (string s);	//for the cases : 0x AB CD or oxAB CD or ox ABCD
int hex_to_int (char a);
int registernumber (string s);
int isregister_or_isimmidiate (string s);//wat for number
